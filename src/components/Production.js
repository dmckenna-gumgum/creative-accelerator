import React, { useState, useEffect } from "react";
import { Button } from '@swc-react/button';
import { FieldLabel } from '@swc-react/field-label';

function Production() {
    return (
        <div className="plugin-menu plugin-menu--production plugin-flex--flex-column" id="production-menu">
            <span> This is where options for readying the document for production will live</span>
        </div>
    );
}

export default Production;
